# Default config for sway
#
# Copy this to ~/.config/sway/config and edit it to your liking.
#
# Read `man 5 sway` for a complete reference.

### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

### Output configuration
#
exec uwsm app -- dunst &

seat * xcursor_theme DMZ-White 24
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
output * bg ~/wallpaper.jpg fill

### Idle configuration
#
# Example configuration:
#
exec swayidle -w \
	     timeout 1200 'swaylock -f -c 000000' \
	     timeout 2400 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
	     before-sleep 'swaylock -f -c 000000'


# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.

### Input configuration
#
# Example configuration:
#
#   input "2:14:SynPS/2_Synaptics_TouchPad" {
#       dwt enabled
#       tap enabled
#       natural_scroll enabled
#       middle_emulation enabled
#   }
#
# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.

### Key bindings
#
# Basics:
#
# Kill focused window
bindsym $mod+Backspace kill

# Start your programs
bindsym $mod+b exec uwsm app -- qutebrowser
bindsym $mod+t exec uwsm app -- alacritty
bindsym XF86Search exec uwsm app -- wmenu-run

# Shut down computer
bindsym $mod+q exec bash -c "dunst-confirm 'Computer will shut down' 'systemctl poweroff'"

# Move your focus around
bindsym $mod+$left exec ~/.config/sway/scripts/cycle_global.sh prev
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right exec ~/.config/sway/scripts/cycle_global.sh next

# Move the focused window with the same, but add Shift
bindsym $mod+Ctrl+$left exec ~/.config/sway/scripts/move_global.sh prev
bindsym $mod+Ctrl+$down move down
bindsym $mod+Ctrl+$up move up
bindsym $mod+Ctrl+$right exec ~/.config/sway/scripts/move_global.sh next

#
# Resizing containers:
#
bindsym $mod+Shift+$right resize shrink width 50px
bindsym $mod+Shift+$down resize grow height 50px
bindsym $mod+Shift+$up resize shrink height 50px
bindsym $mod+Shift+$left resize grow width 50px

# Alerts
# Custom keybind for Dunst to Confirm the action
bindsym $mod+y exec dunstctl action

# Custom keybind for Dunst to Dismiss the notification
bindsym $mod+n exec dunstctl close

#
# Utilities:
#
# Special keys to adjust volume via PulseAudio
bindsym --locked XF86AudioMute exec wpctl set-mute \@DEFAULT_SINK@ toggle && ~/.config/sway/scripts/vol.py
bindsym --locked XF86AudioLowerVolume exec wpctl set-volume \@DEFAULT_SINK@ 5%- && ~/.config/sway/scripts/vol.py
bindsym --locked XF86AudioRaiseVolume exec wpctl set-volume -l 1.0 \@DEFAULT_SINK@ 5%+ && ~/.config/sway/scripts/vol.py
bindsym --locked XF86AudioMicMute exec pactl set-mute \@DEFAULT_SOURCE@ toggle && ~/.config/sway/scripts/vol.py

# Special keys to adjust brightness via brightnessctl
bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%- && ~/.config/sway/scripts/brightness.py
bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+ && ~/.config/sway/scripts/brightness.py

# Special key to take a screenshot with grim
bindsym $mod+s exec grim -g "$(slurp)" - | wl-copy

# Record video
bindsym $mod+v exec bash $HOME/screencast.sh

input "type:keyboard" {
  xkb_layout latam
}


default_border pixel 5
# 1. Set the focused window border to white (and background/text if you like)
# Syntax: client.focused <border> <background> <text> <indicator> <child_border>

client.placeholder #000000 #000000 #ffffff #000000 #000000
client.unfocused #000000 #000000 #ffffff #000000 #000000
client.focused #219ebc #219ebc #ffffff #219ebc #219ebc

# 2. Enable smart borders
# This hides the border when there is only one window on the workspace
smart_borders on

gaps inner 10

bar {
    status_command exec ~/.config/sway/scripts/status.py
    pango_markup enabled

    position top

    colors {
        statusline #ffffff
        background #000000CC
        focused_workspace #000000 #219ebc #ffffff
        inactive_workspace #000000 #000000 #cccccc
    }
}
